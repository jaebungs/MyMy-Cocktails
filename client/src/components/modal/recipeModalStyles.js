import {makeStyles} from '@material-ui/core/styles';

export const recipeModalStyles = makeStyles({
    modalContainer: {
        padding: '2rem'
    },
    modalTitle: {
        fontSize: '2rem',
        textAlign: 'center'
    }
});
