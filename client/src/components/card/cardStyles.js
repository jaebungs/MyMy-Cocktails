import { makeStyles } from "@material-ui/core/styles";

export const cardStyles = makeStyles({
    card: {
        background: '#fff'
    },
    cardTitle: {
        fontWeight: "bold",
        padding: '12px 7px',
        userSelect: 'text'
    },
    chip: {
        margin: '4px'
    }
});